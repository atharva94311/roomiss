# Dependencies
node_modules/

# Next.js build output
.next/
out/
build/
dist/

# Local env files (never commit secrets — see .env.example for the shape)
.env
.env*.local
.env.production

# Vercel
.vercel/

# TypeScript incremental build cache
*.tsbuildinfo
next-env.d.ts

# Editor / OS detritus
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
/tmp/*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# AI tooling local state — never push
.claude/
CLAUDE.md
testone/

# Original design-handoff bundle. Kept on disk for reference, not part of the
# build. Comment this line out if you want it on the remote.
prototype/
.vercel
